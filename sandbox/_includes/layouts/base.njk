<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} | @ak2lab/engine Sandbox</title>
  <meta name="description" content="{{ description | default(site.description) }}">

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&family=Noto+Serif+JP:wght@300;400;500;700&family=Roboto+Mono:wght@400;500;700&display=swap" rel="stylesheet">

  <!-- Lucide Icons -->
  <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>

  {% if useGsap %}
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
  {% endif %}

  {% if useThreeJS %}
  <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
  {% endif %}

  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  {% include "layout-header-nav/layout-header-nav.njk" %}

  {% if sections %}
    {% for section in sections %}
      {% if section.template == "content" %}
        {{ content | safe }}
      {% elif section.template %}
        {% include section.template + ".njk" ignore missing %}
      {% endif %}
    {% endfor %}
  {% else %}
    {{ content | safe }}
  {% endif %}

  {% include "layout-footer-kinetic/layout-footer-kinetic.njk" %}

  {% from "block-search-modal/block-search-modal.njk" import searchModal %}
  {{ searchModal() }}

  <script src="/js/site.js"></script>

  {% if effects %}
  <script>
    {% for effect in effects %}
    AK2.register(new {{ effect }}());
    {% endfor %}
  </script>
  {% endif %}

  {% if useThreeJS %}
  <script>new BgEffectParticle();</script>
  {% endif %}

  {% if useLogoAnimation %}
  <script>new LogoAnimation();</script>
  {% endif %}

</body>
</html>
