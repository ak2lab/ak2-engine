{# ── content section ─────────────────────────────────────────────────────
   Markdown 本文（{{ content }}）をそのまま出力するプレースホルダー。
   sections 配列内でこのタイプが現れた位置に本文が差し込まれる。

   主な用途: hero + 本文 + feature-grid などのハイブリッドページ。
             本文（Markdown body）を YAML セクションの間に挟みたいときに使用。

   出力 HTML 構造:
     （Markdown から変換された HTML がそのまま出力される）

   注意:
     sections なしのページでは base.njk が自動で本文を展開するため
     このセクションタイプは不要。sections を使う場合のみ必要。
     同一ページで複数回使用すると同じ本文が重複して出力される。

   セクションパラメータ: なし（パラメータは不要）
   ──────────────────────────────────────────────────────────────────── #}
{{ content | safe }}
